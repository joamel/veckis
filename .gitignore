node_modules/
dist/
.env
!app/.env
.env.local
.env*.local
.env.test
*.log
.DS_Store

# Expo
app/.expo/
app/android/
app/ios/

# Prisma
backend/prisma/migrations/*.sql.bak

# Claude Code harness
.claude/scheduled_tasks.lock

# Lokalt skräp/sandbox — ska aldrig committas
.claude/settings.json
backend/server.err
sandbox-sounds/
sound-sandbox.mjs

# Nedladdade AAB:er från Actions — hör inte i repot (82 MB styck)
builds/

# Databasdumpar — innehåller användardata, ska ALDRIG committas
backups/

# Experimentbilder och deras resultat — kokbokssidor, inget som hör hemma i repot
experiments/**/bilder/
experiments/**/*-rutor.html

# Granskningsfiler från städskripten (backend/scripts) — lokala arbetsfiler,
# genereras ur databasen och hör inte hemma i repot.
granskning*.txt

# Skriptens minne av dina "nej" — lokalt per maskin.
backend/scripts/.nej-lista.txt
granskning*.txt.föregående

# Granskningsfiler från städskripten — genereras ur databasen, hör inte i repot.
backend/*.txt
backend/*.txt.föregående
