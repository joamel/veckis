services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: veckis
      POSTGRES_PASSWORD: veckis
      POSTGRES_DB: veckis
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U veckis"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
